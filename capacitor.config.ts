import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'PMDMO-Practica_Examen_Ionic',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
